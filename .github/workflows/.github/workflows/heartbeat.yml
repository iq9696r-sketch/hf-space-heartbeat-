name: Heartbeat Both Spaces

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping-fullmax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: browser-actions/setup-chrome@v1
      - name: Visit FullMax Space
        run: |
          pip install selenium
          python -c "
          from selenium import webdriver
          from selenium.webdriver.chrome.options import Options
          import time
          options = Options()
          options.add_argument('--headless')
          options.add_argument('--no-sandbox')
          driver = webdriver.Chrome(options=options)
          driver.get('https://reoo88-fullmax.hf.space')
          time.sleep(10)
          print('FullMax:', driver.title)
          driver.quit()
          "

  ping-b6rn:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: browser-actions/setup-chrome@v1
      - name: Visit B6rn Space
        run: |
          pip install selenium
          python -c "
          from selenium import webdriver
          from selenium.webdriver.chrome.options import Options
          import time
          options = Options()
          options.add_argument('--headless')
          options.add_argument('--no-sandbox')
          driver = webdriver.Chrome(options=options)
          driver.get('https://reoo88-b6rn.hf.space')
          time.sleep(10)
          print('B6rn:', driver.title)
          driver.quit()
          "
